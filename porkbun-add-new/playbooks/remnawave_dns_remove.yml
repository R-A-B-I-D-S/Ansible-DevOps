---
- name: 🗑️ Удалить A-записи неактивных нод Remnawave из DNS
  hosts: localhost
  gather_facts: no

  vars:
    # Работаем напрямую с target_domains — это домены 2-го уровня (apex)
    domains_to_process: "{{ target_domains }}"

  tasks:

    # --- ШАГ 1: Получить список всех нод из Remnawave API ---
    - name: 🌐 Получить все ноды из Remnawave
      uri:
        url: "https://{{ remnawave_domain }}/api/nodes"
        method: GET
        headers:
          Authorization: "Bearer {{ remnawave_token }}"
        return_content: yes
        timeout: 30
      register: remnawave_response
      until: remnawave_response.status == 200
      retries: 3
      delay: 5

    - name: ❌ Проверить ошибку Remnawave API
      fail:
        msg: "Remnawave API вернул ошибку: {{ remnawave_response.json.message | default('Unknown error') }}"
      when: remnawave_response.json is defined and remnawave_response.json.errorCode is defined

    - name: 🧩 Извлечь все ноды
      set_fact:
        all_nodes: "{{ remnawave_response.json.response | list }}"

    - name: 🧩 Извлечь активные ноды (isNodeOnline == true)
      set_fact:
        active_nodes: >-
          {{
            all_nodes
            | selectattr('isNodeOnline', 'equalto', true)
            | list
          }}

    - name: 🧩 Извлечь неактивные ноды (isNodeOnline == false)
      set_fact:
        inactive_nodes: >-
          {{
            all_nodes
            | selectattr('isNodeOnline', 'equalto', false)
            | list
          }}

    - name: 📄 Вывести список активных нод
      debug:
        msg: |-
          АКТИВНЫЕ НОДЫ ({{ active_nodes | length }} шт.):
          {% for node in active_nodes %}
          {{ "%2d" % loop.index }}) {{ node.name }} → {{ node.address }}
          {% endfor %}
      when: active_nodes | length > 0

    - name: 📄 Вывести список неактивных нод
      debug:
        msg: |-
          НЕАКТИВНЫЕ НОДЫ ({{ inactive_nodes | length }} шт.):
          {% for node in inactive_nodes %}
          {{ "%2d" % loop.index }}) {{ node.name }} → {{ node.address }}
          {% endfor %}
      when: inactive_nodes | length > 0

    - name: 🟡 Нет неактивных нод — пропускаем DNS-операции
      debug:
        msg: "Нет неактивных нод в Remnawave. Ничего удалять не нужно."
      when: inactive_nodes | length == 0

    # --- ШАГ 2: Для каждого домена получить все DNS-записи ---
    - name: 🔍 Получить все DNS-записи для домена
      uri:
        url: "https://api.porkbun.com/api/json/v3/dns/retrieve/{{ item }}"
        method: POST
        body:
          apikey: "{{ porkbun_api_key }}"
          secretapikey: "{{ porkbun_secret_api_key }}"
        body_format: json
        return_content: yes
      register: dns_retrieve_results
      ignore_errors: yes
      loop: "{{ domains_to_process }}"
      loop_control:
        label: "{{ item }}"

    # --- ШАГ 3: Проверить ошибки Porkbun API ---
    - name: ❌ Проверить ошибку Porkbun API для домена
      fail:
        msg: "Porkbun API error for {{ item }}: {{ result.json.message | default('Unknown') }}"
      when: result.json is defined and result.json.status != "SUCCESS"
      loop: "{{ domains_to_process }}"
      loop_control:
        label: "{{ item }}"
      vars:
        result: "{{ dns_retrieve_results.results | selectattr('item', 'equalto', item) | first }}"

    # --- ШАГ 4: Найти A-записи, соответствующие неактивным нодам ---
    - name: 🧩 Найти A-записи для удаления (только для apex-домена)
      set_fact:
        records_to_delete_map: >-
          {{
            records_to_delete_map | default({}) | combine({
              item: (
                (
                  (dns_retrieve_results.results | selectattr('item', 'equalto', item) | first | default({}))
                  .json.records | default([])
                )
                | selectattr('type', 'equalto', 'A')
                | selectattr('name', 'equalto', item)  # Только apex-домен
                | selectattr('content', 'in', inactive_nodes | map(attribute='address') | list)
                | list
              )
            })
          }}
      loop: "{{ domains_to_process }}"
      loop_control:
        label: "{{ item }}"

    # --- ДЕБАГ: Вывести найденные записи для удаления ---
    - name: 🐞 DEBUG — Найденные записи для удаления
      debug:
        msg: |-
          Домен: {{ domain }}
          Записей для удаления: {{ records | length }}
          {% for record in records %}
          {{ "%2d" % loop.index }}) {{ record.name }} → {{ record.content }} (ID: {{ record.id }})
          {% endfor %}
      loop: "{{ domains_to_process }}"
      loop_control:
        label: "{{ domain }}"
      vars:
        domain: "{{ item }}"
        records: "{{ records_to_delete_map[domain] | default([]) }}"
      when: records | length > 0

    - name: 🟡 Нет записей для удаления в домене
      debug:
        msg: "ℹ️ В домене {{ domain }} нет A-записей, соответствующих неактивным нодам"
      loop: "{{ domains_to_process }}"
      loop_control:
        label: "{{ domain }}"
      vars:
        domain: "{{ item }}"
        records: "{{ records_to_delete_map[domain] | default([]) }}"
      when: records | length == 0

    # --- ШАГ 5: Подтверждение удаления (опционально) ---
    - name: 🛑 Подтвердите удаление записей
      pause:
        prompt: "Найдено {{ total_records }} записей для удаления. Введите 'yes' для подтверждения, или нажмите Ctrl+C для отмены"
      register: confirmation_pause
      vars:
        total_records: >-
          {% set total = 0 %}
          {% for domain in domains_to_process %}
            {% set records = records_to_delete_map[domain] | default([]) %}
            {% set total = total + records | length %}
          {% endfor %}
          {{ total }}
      when: >-
        {% set any_records = false %}
        {% for domain in domains_to_process %}
          {% if records_to_delete_map[domain] | default([]) | length > 0 %}
            {% set any_records = true %}
          {% endif %}
        {% endfor %}
        {{ any_records }}

    # --- ШАГ 6: Удалить найденные A-записи ---
    - name: 🗑️ Удалить A-записи неактивных нод
      uri:
        url: "https://api.porkbun.com/api/json/v3/dns/delete/{{ domain }}/{{ record.id }}"
        method: POST
        body:
          apikey: "{{ porkbun_api_key }}"
          secretapikey: "{{ porkbun_secret_api_key }}"
        body_format: json
        status_code: 200
      register: delete_results
      loop: >-
        {% set result = [] %}
        {% for domain in domains_to_process %}
          {% for record in records_to_delete_map[domain] | default([]) %}
            {% set result = result.append({'domain': domain, 'record': record}) %}
          {% endfor %}
        {% endfor %}
        {{ result }}
      loop_control:
        loop_var: item
        label: "{{ item.domain }} → {{ item.record.content }}"

    # --- ШАГ 7: Отчет об удалении ---
    - name: ✅ Отчет об удаленных записях
      debug:
        msg: |-
          ✅ Удалено A-записей для домена {{ domain }}: {{ deleted_count }} шт.
          {% for del in domain_delete_results %}
          {{ "%2d" % loop.index }}) {{ del.record.name }} → {{ del.record.content }}
          {% endfor %}
      loop: "{{ domains_to_process }}"
      loop_control:
        label: "{{ domain }}"
      vars:
        domain_delete_results: >-
          {{
            delete_results.results
            | selectattr('item.domain', 'equalto', item)
            | selectattr('changed', 'true')
            | map(attribute='item')
            | list
          }}
        deleted_count: "{{ domain_delete_results | length }}"
      when: domain_delete_results | length > 0

    # --- ШАГ 8: Ошибки при удалении ---
    - name: ⚠️ Ошибки при удалении записей
      debug:
        msg: |-
          ❌ Ошибки при удалении для домена {{ domain }}:
          {% for del in domain_delete_errors %}
          {{ "%2d" % loop.index }}) {{ del.record.name }} → {{ del.record.content }} ({{ del.delete_result.status }}: {{ del.delete_result.status_text }})
          {% endfor %}
      loop: "{{ domains_to_process }}"
      loop_control:
        label: "{{ domain }}"
      vars:
        domain_delete_errors: >-
          {{
            delete_results.results
            | selectattr('item.domain', 'equalto', item)
            | selectattr('failed', 'true')
            | map(attribute='item')
            | list
          }}
      when: domain_delete_errors | length > 0

    # --- ФИНАЛ: Итоговая сводка ---
    - name: 📊 Итоговая сводка
      debug:
        msg: |-
          
          Очистка DNS от неактивных нод завершена.
          
          Всего нод: {{ all_nodes | length }}
          Активных: {{ active_nodes | length }}
          Неактивных: {{ inactive_nodes | length }}
          
          Целевых доменов: {{ domains_to_process | length }}
          
          {% set total_deleted = 0 %}
          {% for domain in domains_to_process %}
            {% set deleted = (delete_results.results | selectattr('item.domain', 'equalto', domain) | selectattr('changed', 'true') | list) | length %}
            {% set total_deleted = total_deleted + deleted %}
          {% endfor %}
          Удалено A-записей: {{ total_deleted }}

      when: true

    - name: 📊 Ничего не удалялось
      debug:
        msg: "ℹ️ Ничего не удалялось — неактивных нод или соответствующих им A-записей не найдено."
      when: >-
        {% set any_records = false %}
        {% for domain in domains_to_process %}
          {% if records_to_delete_map[domain] | default([]) | length > 0 %}
            {% set any_records = true %}
          {% endif %}
        {% endfor %}
        not any_records
