---
- name: 🧹 Удалить A-записи неактивных нод из Porkbun
  hosts: localhost
  gather_facts: no

  vars:
    domains_to_process: "{{ target_domains }}"  # список доменов
    remnawave_api_url: "https://{{ remnawave_domain }}/api/nodes"

  tasks:

    # --- ШАГ 1: Получить список нод из Remnawave ---
    - name: 🌐 Получить список нод из Remnawave
      uri:
        url: "{{ remnawave_api_url }}"
        method: GET
        headers:
          Authorization: "Bearer {{ remnawave_token }}"
        return_content: yes
        timeout: 30
      register: remnawave_response
      until: remnawave_response.status == 200
      retries: 3
      delay: 5

    - name: ❌ Проверить ошибку Remnawave API
      fail:
        msg: "Remnawave API error: {{ remnawave_response.json.message | default('Unknown error') }}"
      when: remnawave_response.json.errorCode is defined

    - name: 🧩 Выделить активные IP
      set_fact:
        active_ips: >-
          {{
            remnawave_response.json.response
            | selectattr('isNodeOnline', 'equalto', true)
            | map(attribute='address')
            | map('trim')
            | list
            | unique
          }}

    - name: 📊 Показать активные IP
      debug:
        msg: "Активные IP: {{ active_ips }}"

    # --- ШАГ 2: Получить DNS-записи для всех доменов ---
    - name: 🔍 Получить все DNS-записи
      uri:
        url: "https://api.porkbun.com/api/json/v3/dns/retrieve/{{ item }}"
        method: POST
        body:
          apikey: "{{ porkbun_api_key }}"
          secretapikey: "{{ porkbun_secret_api_key }}"
        body_format: json
        return_content: yes
      register: dns_results
      loop: "{{ domains_to_process }}"
      loop_control:
        label: "{{ item }}"

    - name: 🧩 Собрать все A-записи для каждого домена
      set_fact:
        dns_a_map: >-
          {{
            dns_a_map | default({}) | combine({
              item: (
                (
                  (dns_results.results | selectattr('item', 'equalto', item) | first).json.records
                  | selectattr('type', 'equalto', 'A')
                  | selectattr('name', 'equalto', item)
                  | list
                )
              )
            })
          }}
      loop: "{{ domains_to_process }}"
      loop_control:
        label: "{{ item }}"

    - name: 🐞 DEBUG — dns_a_map (все A-записи)
      debug:
        var: dns_a_map

    # --- ШАГ 3: Определить неактивные IP ---
    - name: 🧩 Найти неактивные IP по доменам
      set_fact:
        inactive_ips_map: >-
          {{
            inactive_ips_map | default({}) | combine({
              domain: (
                (dns_a_map[domain] | map(attribute='content') | map('trim') | list)
                | difference(active_ips)
              )
            })
          }}
      loop: "{{ domains_to_process }}"
      loop_control:
        loop_var: domain
        label: "{{ domain }}"

    - name: 📋 Показать неактивные IP
      debug:
        msg: |-
          Домен: {{ domain }}
          Неактивные IP: {{ inactive_ips_map[domain] }}
      loop: "{{ domains_to_process }}"
      loop_control:
        label: "{{ domain }}"

    # --- ШАГ 4: Удалить неактивные A-записи ---
    - name: 🗑️ Удалить A-записи с неактивными IP
      vars:
        records_to_delete: "{{ dns_a_map[domain] | selectattr('content', 'in', inactive_ips_map[domain]) | list }}"
      loop: "{{ domains_to_process }}"
      loop_control:
        loop_var: domain
        label: "{{ domain }}"
      when: inactive_ips_map[domain] | length > 0
      block:
        - name: 🚮 Удаление через Porkbun API
          uri:
            url: "https://api.porkbun.com/api/json/v3/dns/delete/{{ domain }}/{{ record.id }}"
            method: POST
            body:
              apikey: "{{ porkbun_api_key }}"
              secretapikey: "{{ porkbun_secret_api_key }}"
            body_format: json
            status_code: 200
          loop: "{{ records_to_delete }}"
          loop_control:
            loop_var: record
            label: "{{ domain }} → {{ record.content }}"
          register: delete_results

        - name: ✅ Отчёт по удалённым записям
          debug:
            msg: |-
              ✅ Удалено записей в {{ domain }}: {{ delete_results.results | length }}
              {% for rec in delete_results.results %}
              - {{ domain }} → {{ rec.item.content }}
              {% endfor %}

    - name: ℹ️ Нет записей для удаления
      debug:
        msg: "Все DNS A-записи соответствуют активным нодам — удалять нечего."
      when: inactive_ips_map | dict2items | map(attribute='value') | map('length') | sum == 0
